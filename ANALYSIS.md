# Pdfsim 代码分析 & QuoteAI落地评估

## 一、项目概览

PDF图纸相似性查找API服务，v3.0（PG持久化版），FastAPI框架。

**核心流程**：上传PDF → 渲染图像 → 提取特征 → 存PG+FAISS → 查询时融合评分排序

**评分权重**：图像结构70% + OCR全文20% + 工程关键字段10%

## 二、架构拆解

### 2.1 特征提取（feature_extractor.py）— 核心模块

**图像特征（512维向量）**：
- 有torch时：ResNet-50(256维) + 全局pHash(64维) + HOG边缘(192维) = 512维
- 无torch时：全局pHash(64维) + 分块pHash 3x3(108维) + HOG(128/192维) → pad到512维

**图像预处理流水线**：
1. 白边裁剪（阈值240二值化+boundingRect）
2. 纠偏（Hough直线检测+中值角度旋转，>5°放弃）
3. 尺寸归一化（max 1600px）
4. CLAHE对比度增强
5. 自适应阈值+形态学开运算

**OCR文本**：
- 优先PDF原生文本（PyMuPDF提取）
- 文本<30字符时回退PaddleOCR（仅前3页）
- 文本相似度：rapidfuzz token_set_ratio

**工程关键字段（正则+关键词匹配）**：
- 材料：SUS304/316L/Q235/6061等30+种
- 工艺：车削/铣削/CNC/线切割等18种
- 表面处理：阳极氧化/镀锌/喷砂等15种
- 公差：±0.xx / +0.xx / GB/T标准
- 尺寸：NxN / ΦN / MN
- 字段权重：材料35% + 工艺25% + 表面20% + 公差10% + 尺寸10%

**去重**：首页缩略图SHA-256内容哈希

### 2.2 存储（db.py + index_store.py）

- **PostgreSQL**（asyncpg连接池）：
  - `drawing_docs`表：元数据（文件名/OCR文本/材料/工艺等16字段+JSONB扩展）
  - `drawing_vectors`表：doc_id + page_index + bytea向量
- **FAISS IndexFlatIP**：内存运行时索引，启动时从PG全量加载
- 删除文档：PG删除 + FAISS全量重建

### 2.3 API（main.py）

| 端点 | 方法 | 功能 |
|------|------|------|
| /api/v1/documents | POST | 上传PDF入库 |
| /api/v1/search | POST | 上传PDF查询相似 |
| /api/v1/search/{doc_id} | POST | 按已有doc_id查相似 |
| /api/v1/documents | GET | 列表文档 |
| /api/v1/documents/{doc_id} | GET/DELETE | 查看/删除文档 |
| /api/v1/health | GET | 健康检查 |

### 2.4 依赖

| 依赖 | 用途 | 大小影响 |
|------|------|----------|
| torch+torchvision | ResNet-50深度特征 | ~2GB |
| PyMuPDF | PDF渲染 | 轻量 |
| opencv-python-headless | 图像处理 | 中等 |
| PaddleOCR | 中文OCR | ~500MB |
| faiss-cpu | 向量索引 | 轻量 |
| asyncpg | PG异步驱动 | 轻量 |
| rapidfuzz | 文本模糊匹配 | 轻量 |

## 三、优势

1. **模块化清晰**：特征提取/存储/API分层，易维护
2. **融合评分合理**：图像为主(70%)，文本和字段为辅，符合图纸相似性判断逻辑
3. **PG持久化**：重启不丢数据，FAISS从PG加载恢复
4. **图纸专用预处理**：白边裁剪、纠偏、CLAHE等都是针对工程图纸的
5. **去重机制**：内容哈希避免重复入库
6. **关键字段覆盖**：材料/工艺/表面处理/公差/尺寸，制造业常用维度都有
7. **双模特征**：有torch走深度特征，无torch走传统特征，灵活

## 四、问题与风险（2万张图纸场景）

### 4.1 🔴 关键问题

| # | 问题 | 影响 | 建议 |
|---|------|------|------|
| 1 | **FAISS IndexFlatIP全量扫描** | 2万张×平均2页=4万向量，每次搜索O(n)，~50ms/次还行但增长后变慢 | 换IVFFlat或HNSW索引，或用pgvector |
| 2 | **无批量导入** | 只有单文件上传API，2万张PDF需批量导入 | 加batch import API（目录扫描/队列） |
| 3 | **同步处理阻塞** | 上传+OCR+特征提取在请求线程内，大PDF可能30s+ | 改为后台任务+状态查询 |
| 4 | **删除需全量重建FAISS** | 2万向量重建耗时秒级 | 用FAISS IDSelector或换IVF索引 |
| 5 | **启动加载全量向量到内存** | 4万×512×4bytes ≈ 80MB，还行但10万+就大了 | 改pgvector按需检索 |

### 4.2 🟡 改进建议

| # | 问题 | 建议 |
|---|------|------|
| 6 | ResNet-50 ImageNet预训练对图纸效果一般 | 可试CLIP/ViT，或微调；传统特征反而对线条图更稳定 |
| 7 | OCR仅前3页 | 工程图纸通常1-2页，问题不大，但可配置 |
| 8 | 无缩略图API | 前端展示需缩略图，代码里有thumbnail函数但没暴露API |
| 9 | 关键词列表硬编码 | 应抽为配置文件，不同行业关键词不同 |
| 10 | 无认证 | 生产环境需加API Key或JWT |

### 4.3 🟢 可接受的点

- torch 2GB：CPU版可接受，Docker镜像大小可接受
- PaddleOCR 500MB：中文图纸必须，无法绕过
- 512维向量：精度和性能的平衡点，OK

## 五、QuoteAI落地方案

### 5.1 阶段一：快速验证（1-2天）

1. **部署Pdfsim服务**：Docker部署到新客户服务器(110.40.147.171)
   - PG用现有H3SaasAI的PostgreSQL
   - CPU模式运行（不用GPU）
2. **批量导入脚本**：写Python脚本扫描PDF目录，并行调用上传API
3. **搜索质量测试**：用10-20张典型图纸测试Top10结果，人工评估

### 5.2 阶段二：QuoteAI集成（2-3天）

1. **QuoteAI新增相似图纸查找接口**：
   - 报价时上传PDF → 调Pdfsim search API → 返回Top10相似图纸
   - 相似图纸关联历史报价（从quote_experiences取）
2. **前端展示**：搜索结果卡片（缩略图+相似度+材料/工艺匹配）
3. **关键词配置化**：根据QuoteAI已有process_items表动态生成关键词列表

### 5.3 阶段三：性能优化（按需）

1. FAISS → IVFFlat索引（2万+向量后）
2. 批量导入加进度条+断点续传
3. 异步处理改后台任务
4. 考虑pgvector替换FAISS

## 六、关键数据估算

| 指标 | 估算 |
|------|------|
| 2万张PDF入库时间 | ~20000 × 2s/张 ≈ 11小时（含OCR，单线程） |
| 并行5线程 | ~2-3小时 |
| 向量索引内存 | 4万向量 × 512维 × 4字节 ≈ 80MB |
| PG存储 | 2万文档元数据 ~200MB + 向量bytea ~320MB ≈ 500MB |
| 单次查询延迟 | FAISS搜索<50ms + PG取元数据<20ms ≈ 100ms |
| Docker镜像 | ~3.5GB（torch CPU + PaddleOCR + opencv） |

## 七、结论

**代码质量**：中等偏上，结构清晰，功能完整，可直接作为基础使用。

**与之前评估的方案二对齐**：这个实现基本就是"结构化特征嵌入"方案的落地版本——pHash/ResNet做视觉嵌入，关键词做结构化字段，融合评分。

**建议**：先按阶段一部署验证搜索质量，质量达标后再做QuoteAI集成。2万张图纸的场景下，当前架构够用，IVF优化可以等量级上来再说。
